import React from "react";
import Formulario from "../component/formulario";

const Edit = () => {
	return (
		<div className="mt-5">
			<div className="container">
				<Formulario />
			</div>
		</div>
	);
};

export default Edit;